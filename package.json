{
  "name": "moon-bites",
  "version": "0.1.0",
  "private": true,
  "description": "Moon Bites — Grace Moments. Sitio estático (HTML/CSS/JS plano, sin build) para bitesworld.net.",
  "scripts": {
    "dev": "npx --yes serve site -l 8000",
    "dev:python": "python3 -m http.server 8000 --directory site"
  }
}
